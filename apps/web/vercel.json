{
  "installCommand": "npm install --legacy-peer-deps --no-workspaces",
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "framework": "nuxtjs",
  "build": {
    "env": {
      "NODE_VERSION": "20",
      "NPM_FLAGS": "--legacy-peer-deps",
      "CI": "true"
    }
  },
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}
